
/*
import { render, screen } from '@testing-library/react'
import ProductSpecification from './ProductSpecification'
import { BrowserRouter as Route } from 'react-router-dom'
import { RecoilRoot } from 'recoil';
import React from 'react'
import { cart } from '../../recoil/atoms';
*/

test('Scan article button', () => {
    /*
    render(<Route><React.StrictMode><RecoilRoot><ProductSpecification /></RecoilRoot></React.StrictMode></Route>)
    const theButton = screen.getByRole('button', {
        name: /Lägg till i kundvagnen/i})
    expect(theButton).toBeDefined()
    console.log(cart.key)
    */
})
